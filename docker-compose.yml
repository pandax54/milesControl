services:
  db:
    image: postgres:16-alpine
    container_name: milescontrol-db
    ports:
      - "5433:5432"
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: secret
      POSTGRES_DB: milescontrol
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
