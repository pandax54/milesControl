2. Test description/mock value corrected — "more than 3 consecutive failures" test uses mock value 4 (strictly above threshold)

Verification: 951 tests pass, TypeScript compiles cleanly, coverage 98.46% (above 80% threshold).

## Task 3.12 — Miles Calendar Page (completed 2026-03-21)

Files created:
- src/lib/services/miles-calendar.service.ts — Service with listCalendarEvents, listUpcomingCalendarEvents, listEventsByMonth, groupEventsByMonth, PROMO_TYPE_LABELS. UTC-consistent date filtering.
- src/lib/services/miles-calendar.service.test.ts — 13 unit tests covering all service functions and groupEventsByMonth edge cases
- src/components/promotions/miles-calendar-event-card.tsx — Card component showing event title, date range, programs, expected promo type badge, description, and historical note
- src/components/promotions/miles-calendar-view.tsx — View component that splits events into upcoming/past sections, groups by month, renders MonthSection grids
- src/app/(dashboard)/promotions/calendar/page.tsx — Server component page at /promotions/calendar

Key implementation details:
- UTC-consistent date comparisons throughout (Date.UTC, setUTCHours) to avoid off-by-one timezone errors
- Events grouped by month using reduce (no for...of loops, no non-null assertions)
- Import order: external → @/ internal → relative
- Upcoming events shown first, past events with reduced opacity

Review: APPROVED WITH OBSERVATIONS — all 5 minor issues fixed before commit.
All 964 tests pass. TypeScript compiles cleanly.

## Task 3.12 — Review Fixes Applied (2026-03-21)

Review file: tasks/prd-milescontrol/3.12_task_review.md — Status: APPROVED

Minor issues resolved (commit 483cb8f):
1. Magic number `100` extracted to `MAX_CALENDAR_PAGE_EVENTS = 100` constant in calendar page
2. Duplicated UTC midnight boundary (`new Date(); today.setUTCHours(0,0,0,0)`) extracted to exported `getUtcMidnightToday()` in miles-calendar.service.ts — used by both miles-calendar-event-card.tsx and miles-calendar-view.tsx (and internally by buildWhereClause)

Verification: 964 tests pass, TypeScript compiles cleanly, coverage thresholds met.

## Task 3.12 — Final Review Verification (2026-03-21)

Review file: tasks/prd-milescontrol/3.12_task_review.md — Status: APPROVED (final)

All prior review recommendations were already resolved in commit 483cb8f:
1. Magic number `100` extracted to `MAX_CALENDAR_PAGE_EVENTS = 100`
2. Duplicated UTC midnight boundary extracted to exported `getUtcMidnightToday()` in service

Verification checks:
- `npx tsc --noEmit` — PASS
- `pnpm test` — PASS (964 tests, 0 failing)
- `pnpm run test:coverage` — PASS (above 80% threshold on all metrics)
- `miles-calendar.service.ts` — 100% coverage across all metrics

Review file updated to reflect final approved state with no outstanding issues.

## Task 4.0 — Alert Configuration UI (completed 2026-03-21)

Files created:
- src/lib/validators/alert-config.schema.ts — Zod schemas for create/update/delete/toggle with ALERT_CHANNELS and PROMO_TYPES constants
- src/lib/services/alert-config.service.ts — CRUD service: list, get, create, update, toggle, delete. Includes listAllActiveAlertConfigs for use by task 4.1 matching engine.
- src/lib/services/alert-config.service.test.ts — 14 unit tests covering all CRUD operations and error cases
- src/actions/alerts.ts — Server Actions: addAlertConfig, editAlertConfig, removeAlertConfig, setAlertConfigActive
- src/components/alerts/alert-config-form-fields.tsx — Shared form fields with channel multi-select, promo type checkboxes, program names, threshold inputs
- src/components/alerts/alert-config-form-dialog.tsx — Create dialog with form
- src/components/alerts/edit-alert-config-dialog.tsx — Edit dialog pre-populated from existing config
- src/components/alerts/alert-config-card.tsx — Card with active/paused toggle, edit, delete. Shows channels, program filters, promo type filters, thresholds.
- src/components/alerts/delete-alert-config-button.tsx — Delete button with confirmation dialog
- src/components/alerts/toggle-alert-config-button.tsx — Active/Paused toggle button
- src/app/(alerts)/alerts/page.tsx — Server-rendered page at /alerts. Lists active/paused rules, empty state.
- src/components/app-sidebar.tsx — Added "Alerts" nav group with Alert Rules link

All 977 tests pass. TypeScript compiles cleanly. Coverage thresholds met.

## Task 4.0 — Review Fixes (2026-03-21)

Review file: tasks/prd-milescontrol/4.0_task_review.md — Status: APPROVED
No critical, major, or minor issues found. No fixes required.
Verification: 977 tests pass, TypeScript compiles cleanly, coverage thresholds met.

## Task 4.1 — Review Fixes (2026-03-21)

Review file: tasks/prd-milescontrol/4.1_task_review.md — Status: APPROVED WITH OBSERVATIONS
Major issue resolved: integrated processNewPromotions() into runAllScrapers() post-storage step.
- scrape-promos.service.ts — import prisma + processNewPromotions; after all scrapers run, fetch new promotions (createdAt >= runStartTime) and call processNewPromotions; add alertMatchResult to ScrapePromosResult
- scrape-promos.service.test.ts — mocked prisma.promotion.findMany + processNewPromotions; 4 new tests covering alert matching trigger, skip when no creates, skip when query empty, graceful failure
- scrape-promos route.ts — expose alertMatchResult in cron response
Verification: 1025 tests pass, TypeScript compiles cleanly, coverage thresholds met.

## Task 4.2 — Telegram Bot (completed 2026-03-21)

Files created:
- src/lib/integrations/telegram.ts — sendMessage, sendAlertNotification, escapeHtml. Native fetch with 3-retry logic and 5s delay. HTML parse mode.
- src/lib/integrations/telegram.test.ts — 12 tests covering escapeHtml, sendMessage (retry logic, network errors), sendAlertNotification
- src/lib/services/telegram-notification.service.ts — findUserByChatId (via AlertConfig.telegramChatId), sendTelegramAlerts, getTopPromotionsForBot, getUserAlertConfigs
- src/lib/services/telegram-notification.service.test.ts — 13 tests covering all service functions
- src/app/api/webhook/telegram/route.ts — POST handler with CRON_SECRET validation, command routing: /start (show chat ID + instructions), /alerts (list active rules), /promos (top 5 active promos), /calc (cost per milheiro), unknown command fallback
- src/app/api/webhook/telegram/route.test.ts — 22 tests covering authorization, all 4 commands, edge cases

Key design decisions:
- telegramChatId stored on AlertConfig (already in schema); user lookup via findFirst on AlertConfig.telegramChatId
- /start shows user their chat ID to paste into the app UI (no deep-link needed)
- Webhook secret uses CRON_SECRET header X-Telegram-Bot-Api-Secret-Token
- Always returns 200 to Telegram to prevent retries on processing errors

Verification: 1067 tests pass, TypeScript compiles cleanly, coverage thresholds met.

## Task 4.3 — Review Fixes (2026-03-22)

Review file: tasks/prd-milescontrol/4.3_task_review.md — Status: APPROVED
Optional recommendation applied: added inline comment in sendEmailAlerts() noting silent degradation when RESEND_API_KEY is not configured.
Verification: npx tsc --noEmit PASS, pnpm test PASS (1094 tests), pnpm build PASS, coverage thresholds met.

## Task 4.5 — Review Fixes (2026-03-22)

Review file: tasks/prd-milescontrol/4.5_task_review.md — Status: APPROVED
Minor issues resolved (commit 50287d7):
1. Added missing VAPID keys test in web-push.test.ts using vi.resetModules() + vi.doMock() pattern
2. Created push-notification-toggle.test.tsx — 12 tests covering unsupported/denied/subscribed/unsubscribed states, subscribe/unsubscribe flows, permission denial, and fetch error handling
Recommendation applied: expanded 410/404 comment in web-push.ts with RFC 8030 §8.1 reference.
Verification: 1152 tests pass, TypeScript compiles cleanly, coverage 98.19% (all thresholds met), build PASS.
