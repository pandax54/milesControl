<!-- BEGIN:nextjs-agent-rules -->

## Project (check `milescontrol-project` skill for full details)

Miles & points management platform for the Brazilian market. Next.js 16+ App Router, PostgreSQL via Prisma, TypeScript strict mode.

## Standards & Conventions (check skills before coding)

- **Code standards** (naming, functions, types, formatting) → check `code-standards-reference` skill
- **Testing** (structure, coverage, mocking, assertions) → check `testing-reference` skill
- **Database** (DataSource, entities, migrations, repository pattern) → check `database-typeorm-config` skill
- **REST/HTTP** (routing, status codes, validation, middleware) → check `rest-api-reference` skill
- **Node.js/TypeScript** (strict mode, imports, Koa, Docker, env config) → check `nodejs-typescript-reference` skill
- **Logging** (Pino, levels, redaction, structured logging) → check `logging-reference` skill
- **Command safety** (destructive ops, migration gates, approval flow) → check `command-safety` skill
<!-- END:nextjs-agent-rules -->
