# Review: Task 3.12 - Miles Calendar Page

**Reviewer**: AI Code Reviewer
**Date**: 2026-03-21
**Task file**: tasks.md (task 3.12)
**Status**: APPROVED

## Summary

Task 3.12 implements the miles calendar page — a calendar view with expected promotional periods and historical notes. The implementation consists of a service layer (`miles-calendar.service.ts`), two UI components (`miles-calendar-event-card.tsx`, `miles-calendar-view.tsx`), and a Next.js App Router server component page (`/promotions/calendar/page.tsx`). The sidebar navigation was also updated with a `Miles Calendar` link.

A prior review pass identified two minor issues (magic number `100` in the page component and duplicated UTC midnight boundary computation across the card and view components). Both were fully resolved in commit `483cb8f`:

- `MAX_CALENDAR_PAGE_EVENTS = 100` extracted as a named constant in `page.tsx`.
- `getUtcMidnightToday()` extracted and exported from the service; both `miles-calendar-event-card.tsx` and `miles-calendar-view.tsx` now delegate to this single source of truth.

All 964 tests pass, TypeScript type checking passes with zero errors, and `miles-calendar.service.ts` achieves 100% coverage across all four metrics. The code is clean, well-structured, and satisfies all task requirements.

## Files Reviewed

| File | Status | Issues |
| ---- | ------ | ------ |
| `src/lib/services/miles-calendar.service.ts` | OK | 0 |
| `src/lib/services/miles-calendar.service.test.ts` | OK | 0 |
| `src/components/promotions/miles-calendar-event-card.tsx` | OK | 0 |
| `src/components/promotions/miles-calendar-view.tsx` | OK | 0 |
| `src/app/(dashboard)/promotions/calendar/page.tsx` | OK | 0 |

## Issues Found

### Critical Issues

No critical issues found.

### Major Issues

No major issues found.

### Minor Issues

No minor issues found.

## Positive Highlights

- **Both prior review recommendations fully applied**: The magic number and the duplicated UTC boundary computation were both addressed cleanly in a single follow-up commit. The refactoring approach — exporting `getUtcMidnightToday()` from the service rather than creating a separate utility module — is the minimal, correct solution.

- **UTC-consistent date handling throughout**: `buildWhereClause` uses `Date.UTC(year, month - 1, 1)` for month boundaries; `getUtcMidnightToday()` uses `setUTCHours(0, 0, 0, 0)`; `groupEventsByMonth` reads dates via `getUTCFullYear()` and `getUTCMonth()`; `Intl.DateTimeFormat` is called with `timeZone: 'UTC'`. All date operations are self-consistent and free of timezone shift bugs.

- **Functional style in `groupEventsByMonth`**: Uses `events.reduce<Map<string, EventsByMonth>>(...)` correctly, avoiding `for` loops and non-null assertions. The result is type-safe and idiomatic.

- **100% service coverage**: Every exported function is tested, including edge cases for the empty-array path, custom limits, the no-limit path, year/month filtering, and the new `getUtcMidnightToday` utility (tested indirectly via the `upcomingOnly` path and via `vi.useFakeTimers()` / `vi.useRealTimers()` cleanup).

- **Correct Vitest patterns**: The test file uses `vi.mocked()` for typed mock access, `vi.useFakeTimers()` / `vi.useRealTimers()` cleanup for time-dependent tests, and `vi.clearAllMocks()` in `beforeEach`. This matches project conventions exactly.

- **Correct Pino usage**: The single log call follows the data-first, message-last pattern (`logger.debug({ count, filters }, 'Fetched calendar events')`) and uses `debug` level appropriately for a query result.

- **Type-safe badge variant map**: `TYPE_BADGE_VARIANTS: Record<PromoType, ...>` is exhaustive — if `PromoType` gains a new value the compiler will surface the gap immediately.

- **Null safety for `expectedType`**: The card guards `event.expectedType &&` before indexing into `TYPE_BADGE_VARIANTS`, correctly handling the nullable Prisma field.

- **Past events in reverse chronological order**: `pastGroups.reverse()` in `miles-calendar-view.tsx` line 46 is a well-considered UX decision — most recent past events appear closest to the present.

- **UTC-aware display formatting**: `Intl.DateTimeFormat` is called with `timeZone: 'UTC'` and the same-month branch uses `getUTCDate()` / `getUTCMonth()`, ensuring stored UTC midnight dates render without timezone shift for any user.

- **PRD completeness**: Both required features are present — a calendar view of expected promotional periods and historical notes displayed with a distinct visual treatment (muted info block with an `Info` icon).

- **Sidebar navigation**: `app-sidebar.tsx` was updated with the `Miles Calendar` link pointing to `/promotions/calendar`, making the page discoverable in the app shell.

- **`tasks.md` updated**: Task 3.12 is correctly marked `[x]` as complete.

## Standards Compliance

| Standard | Rule File | Status |
| -------- | --------- | ------ |
| Code Standards | code-standards.md | OK |
| TypeScript/Node.js | nodejs-typescript.md | OK |
| REST/HTTP (Koa) | rest-http.md | N/A — Next.js App Router, no API routes added |
| Logging (Pino) | logging.md | OK |
| Testing (Vitest) | testing.md | OK |

## Verification Results

| Check | Result |
| ----- | ------ |
| `npx tsc --noEmit` | PASS |
| `pnpm test` | PASS — 964 passing, 0 failing |
| Coverage (80% threshold) | PASS — 98.49% statements, 93.01% branches, 98.17% functions, 98.5% lines |
| `miles-calendar.service.ts` coverage | PASS — 100% across all metrics |

## Recommendations

No further recommendations. All issues identified in the prior review pass have been resolved.

## Verdict

The implementation is functionally correct, well-tested, UTC-safe, and satisfies all task requirements. All minor issues from the first review pass were resolved in the follow-up commit. The code is production-ready.
