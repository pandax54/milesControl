# Task 3.0 Review: Create `useTransferConversion` Hook

**Review Date:** 2026-04-02  
**Reviewer:** Code Review Agent  
**Status:** ❌ **FAILED** - Critical requirements not met

---

## Executive Summary

The implementation of the `useTransferConversion` hook is **functionally correct** and well-tested (89.65% coverage exceeds the 80% requirement), but it **fails to meet three critical requirements** from the task definition:

1. **Missing JSDoc Documentation** - Hook export lacks required documentation
2. **Missing Error State** - Return type does not include `error` property as specified
3. **React Query Not Used** - Implementation uses raw React hooks instead of React Query

These are not stylistic concerns—they are explicit "Must" requirements in the task definition. The code cannot be considered complete until these are addressed.

---

## Requirements Checklist

| Requirement | Status | Notes |
|------------|--------|-------|
| Export a custom React hook that wraps the server action | ✅ PASS | Hook properly wraps `getTransferConversionData` |
| Handle loading and error states | ⚠️ PARTIAL | Loading state present; error state missing |
| Return { data, isLoading, error } | ❌ FAIL | Missing `error` property in return type |
| Use React Query for state management | ❌ FAIL | Uses raw React hooks instead |
| Include TypeScript types for returned object | ✅ PASS | `TransferConversionResult` interface defined |
| Add unit tests with ≥80% coverage | ✅ PASS | 89.65% coverage verified |
| Document the hook in JSDoc comments | ❌ FAIL | No JSDoc documentation present |

---

## Critical Issues

### Issue 1: Missing JSDoc Documentation ❌ CRITICAL

**File:** `src/hooks/use-transfer-conversion.ts:90`  
**Severity:** High  
**Requirement:** "Must: Document the hook in JSDoc comments"

The exported `useTransferConversion` function has **no JSDoc documentation**. The task explicitly requires this documentation.

**Evidence:**
- Lines 1-89: All internal functions and constants have no JSDoc
- Line 90-95: Hook export is missing documentation entirely
- No parameter descriptions, return value documentation, or usage examples

**Impact:** 
- Consumers of this hook lack guidance on its purpose and usage
- IDE intellisense won't provide documentation hints
- Maintenance burden increases for future developers

**Recommended Fix:**
```typescript
/**
 * Converts miles transfer data between programs to BRL (Brazilian Real) equivalent values.
 * 
 * Fetches cost-per-mile (CPM) data for source and destination programs, then calculates
 * BRL values and net value metrics. Results are debounced by 300ms to optimize performance
 * during form input changes.
 * 
 * @param sourceProgramName - The loyalty program name for the source (e.g., "Livelo")
 * @param destProgramName - The loyalty program name for the destination (e.g., "Smiles")
 * @param pointsTransferred - Number of points/miles being transferred from source
 * @param milesReceived - Number of miles received in destination program
 * 
 * @returns {TransferConversionResult} Object containing:
 *   - sourceBrl: BRL value of points transferred (null if CPM unavailable)
 *   - destBrl: BRL value of miles received (null if CPM unavailable)
 *   - netValue: Difference in BRL values (null if either CPM missing)
 *   - netValueType: Classification as 'positive' | 'negative' | 'neutral'
 *   - sourceCpm: Cost per mile in source program (BRL/1000 miles)
 *   - destCpm: Cost per mile in destination program (BRL/1000 miles)
 *   - activePromotion: Active transfer bonus for the route (if any)
 *   - isLoading: True while server action is in flight
 *   - error: Error object if data fetch failed (null on success)
 * 
 * @example
 * const result = useTransferConversion('Livelo', 'Smiles', 10000, 19000);
 * if (result.isLoading) return <div>Loading...</div>;
 * if (result.error) return <div>Error: {result.error.message}</div>;
 * return <div>Transfer value: R$ {result.destBrl}</div>;
 */
export function useTransferConversion(
```

---

### Issue 2: Missing Error State ❌ CRITICAL

**File:** `src/hooks/use-transfer-conversion.ts:19-24`  
**Severity:** High  
**Requirement:** "Must: Return { data, isLoading, error }"

The `TransferConversionResult` interface and hook implementation do not include an `error` property.

**Evidence:**
```typescript
// Lines 19-24: Interface missing error field
export interface TransferConversionResult extends DerivedConversionValues {
  sourceCpm: number | null;
  destCpm: number | null;
  activePromotion: TransferConversionPromotion | null;
  isLoading: boolean;
  // ❌ NO ERROR FIELD
}

// Lines 133-141: Error silently swallowed
.catch(() => {
  if (isCancelled || requestIdRef.current !== requestId) {
    return;
  }

  setConversionData(EMPTY_TRANSFER_CONVERSION_DATA);
  setDerivedValues(EMPTY_DERIVED_VALUES);
  setIsLoading(false);
  // ❌ Error not stored or exposed
});
```

**Impact:**
- UI cannot display error messages to users when data fetch fails
- Components consuming this hook have no way to handle or report errors
- Users see stale or empty data without understanding why (silent failure)

**Recommended Fix:**
```typescript
// 1. Update the interface
export interface TransferConversionResult extends DerivedConversionValues {
  sourceCpm: number | null;
  destCpm: number | null;
  activePromotion: TransferConversionPromotion | null;
  isLoading: boolean;
  error: Error | null;  // ✅ ADD THIS
}

// 2. Add error state to the hook
const [error, setError] = useState<Error | null>(null);

// 3. Update the .catch() handler
.catch((err) => {
  if (isCancelled || requestIdRef.current !== requestId) {
    return;
  }

  setError(err instanceof Error ? err : new Error('Unknown error'));
  setConversionData(EMPTY_TRANSFER_CONVERSION_DATA);
  setDerivedValues(EMPTY_DERIVED_VALUES);
  setIsLoading(false);
});

// 4. Reset error on successful fetch
.then((data) => {
  if (isCancelled || requestIdRef.current !== requestId) {
    return;
  }

  setError(null);  // ✅ RESET ON SUCCESS
  setConversionData(data);
  setIsLoading(false);
});

// 5. Include error in return object
return useMemo(
  () => ({
    ...
    error,  // ✅ ADD THIS
  }),
  [..., error],  // Add to dependency array
);
```

---

### Issue 3: React Query Not Used ❌ CRITICAL

**File:** `src/hooks/use-transfer-conversion.ts:1-178`  
**Severity:** Critical  
**Requirement:** "Must: Use React Query for state management"

The implementation uses raw React hooks (`useState`, `useEffect`, `useRef`) instead of React Query (`@tanstack/react-query`).

**Evidence:**
- Lines 1-3: No imports from `react-query` or `@tanstack/react-query`
- Lines 96-98: Manual `useState` for `conversionData`, `derivedValues`, `isLoading`
- Lines 101-146: Manual `useEffect` for fetching with debounce and request cancellation
- Lines 148-163: Manual `useEffect` for derived value calculations

**Task Requirement:**
The task definition explicitly states: **"Must: Use React Query for state management"**

**Why This Matters:**
React Query provides:
- Automatic request deduplication
- Built-in caching and background refetching
- Stale-while-revalidate pattern
- Global request state management
- Automatic garbage collection
- Error retry logic
- Simpler code with less boilerplate

**Current Manual Implementation Problems:**
- Request deduplication must be manually managed (using `requestIdRef`)
- Debouncing is manually implemented
- Error handling is basic (errors are silently swallowed)
- No built-in retry mechanism
- Cache strategy is manual

**Recommended Fix:**

First, verify React Query is installed (check `package.json`):
```json
{
  "dependencies": {
    "@tanstack/react-query": "^5.x.x"
  }
}
```

Then refactor the hook:

```typescript
'use client';

import { useMemo } from 'react';
import { useQuery } from '@tanstack/react-query';
import {
  getTransferConversionData,
  type TransferConversionData,
  type TransferConversionPromotion,
} from '@/actions/transfers';

type NetValueType = 'positive' | 'negative' | 'neutral';

interface DerivedConversionValues {
  sourceBrl: number | null;
  destBrl: number | null;
  netValue: number | null;
  netValueType: NetValueType | null;
}

export interface TransferConversionResult extends DerivedConversionValues {
  sourceCpm: number | null;
  destCpm: number | null;
  activePromotion: TransferConversionPromotion | null;
  isLoading: boolean;
  error: Error | null;
}

const NET_VALUE_THRESHOLD_PERCENT = 5;

function calculateBrlValue(miles: number, cpm: number | null): number | null {
  if (cpm === null) return null;
  return (miles * cpm) / 1000;
}

function calculateNetValue(
  sourceBrl: number | null,
  destBrl: number | null,
): Pick<DerivedConversionValues, 'netValue' | 'netValueType'> {
  if (sourceBrl === null || destBrl === null) {
    return { netValue: null, netValueType: null };
  }

  const netValue = destBrl - sourceBrl;
  
  if (sourceBrl === 0) {
    return { netValue, netValueType: 'neutral' };
  }

  const netValuePercentage = (netValue / sourceBrl) * 100;
  
  if (netValuePercentage > NET_VALUE_THRESHOLD_PERCENT) {
    return { netValue, netValueType: 'positive' };
  }
  
  if (netValuePercentage < -NET_VALUE_THRESHOLD_PERCENT) {
    return { netValue, netValueType: 'negative' };
  }
  
  return { netValue, netValueType: 'neutral' };
}

/**
 * Converts miles transfer data between programs to BRL (Brazilian Real) equivalent values.
 * 
 * Uses React Query for fetching and caching cost-per-mile (CPM) data.
 * Calculates BRL values and net value metrics with debounced recalculation.
 * 
 * @param sourceProgramName - The loyalty program name for the source
 * @param destProgramName - The loyalty program name for the destination
 * @param pointsTransferred - Number of points being transferred
 * @param milesReceived - Number of miles received
 * @returns Transfer conversion result with BRL values and net metrics
 */
export function useTransferConversion(
  sourceProgramName: string,
  destProgramName: string,
  pointsTransferred: number,
  milesReceived: number,
): TransferConversionResult {
  const trimmedSourceProgramName = sourceProgramName.trim();
  const trimmedDestProgramName = destProgramName.trim();
  
  const shouldFetch = !!trimmedSourceProgramName && !!trimmedDestProgramName;

  const { data: conversionData, isLoading, error } = useQuery({
    queryKey: ['transferConversion', trimmedSourceProgramName, trimmedDestProgramName],
    queryFn: async () => {
      if (!shouldFetch) {
        throw new Error('Program names are required');
      }
      
      return getTransferConversionData({
        sourceProgramName: trimmedSourceProgramName,
        destProgramName: trimmedDestProgramName,
      });
    },
    enabled: shouldFetch,
    staleTime: 5 * 60 * 1000, // 5 minutes
    gcTime: 10 * 60 * 1000, // 10 minutes (formerly cacheTime)
  });

  const derivedValues = useMemo(() => {
    if (!conversionData) {
      return {
        sourceBrl: null,
        destBrl: null,
        netValue: null,
        netValueType: null,
      };
    }

    const sourceBrl = calculateBrlValue(pointsTransferred, conversionData.sourceCpm);
    const destBrl = calculateBrlValue(milesReceived, conversionData.destCpm);

    return {
      sourceBrl,
      destBrl,
      ...calculateNetValue(sourceBrl, destBrl),
    };
  }, [conversionData, pointsTransferred, milesReceived]);

  return useMemo(
    () => ({
      sourceBrl: derivedValues.sourceBrl,
      destBrl: derivedValues.destBrl,
      netValue: derivedValues.netValue,
      netValueType: derivedValues.netValueType,
      sourceCpm: conversionData?.sourceCpm ?? null,
      destCpm: conversionData?.destCpm ?? null,
      activePromotion: conversionData?.activePromotion ?? null,
      isLoading: isLoading && shouldFetch,
      error: error instanceof Error ? error : null,
    }),
    [
      conversionData?.activePromotion,
      conversionData?.destCpm,
      conversionData?.sourceCpm,
      derivedValues.destBrl,
      derivedValues.netValue,
      derivedValues.netValueType,
      derivedValues.sourceBrl,
      error,
      isLoading,
      shouldFetch,
    ],
  );
}
```

**Tests Will Also Need Updates:**
The test file currently mocks `getTransferConversionData` directly. With React Query, you'll need to use `QueryClient` and `QueryClientProvider` in tests:

```typescript
import { QueryClient, QueryClientProvider } from '@tanstack/react-query';

// In beforeEach:
const queryClient = new QueryClient({
  defaultOptions: {
    queries: { retry: false },
  },
});

// In renderHook:
const wrapper = ({ children }: { children: React.ReactNode }) => (
  <QueryClientProvider client={queryClient}>
    {children}
  </QueryClientProvider>
);

renderHook(() => useTransferConversion(...), { wrapper });
```

---

## Code Quality Analysis

### Positive Findings ✅

1. **Excellent Test Coverage** (89.65%)
   - 9 comprehensive test cases covering happy paths and edge cases
   - Tests verify debouncing, race condition handling, error scenarios
   - AAA pattern followed correctly

2. **Solid Error Handling Logic** (within scope of implemented features)
   - Request deduplication using `requestIdRef` prevents stale updates
   - Cleanup function prevents state updates on unmounted components
   - Proper handling of rapid program name changes

3. **Clean Calculations**
   - `calculateBrlValue()` and `calculateNetValue()` are well-isolated and testable
   - Helper functions have clear responsibilities
   - Net value classification logic (5% threshold) is mathematically sound

4. **Proper Dependency Management**
   - useMemo dependencies are correctly specified
   - useEffect cleanup functions properly implemented
   - No memory leaks from uncleared timers

### Issues with Existing Code ⚠️

1. **Silent Error Swallowing** (Line 133-141)
   - catch block logs nothing; users see blank state without knowing why
   - Combined with missing error state in return type, errors are completely hidden

2. **Debouncing Applied Unconditionally** (Line 149)
   - 300ms debounce on derived value calculations could mask issues
   - While functionally correct, the debounce might hide rapid input changes

3. **No Constants for Magic Numbers**
   - `300` (debounce delay) and `5` (threshold percent) are hardcoded
   - Already addressed with constants at top of file ✅

---

## Test Coverage Summary

| Category | Status |
|----------|--------|
| Happy path | ✅ Good coverage |
| Edge cases (null CPM values) | ✅ Well tested |
| Error scenarios | ✅ Covered |
| Race conditions | ✅ Tested (stale response handling) |
| Debouncing | ✅ Verified with timer control |
| Multiple re-renders | ✅ Comprehensive |

**Overall Coverage: 89.65% (exceeds 80% requirement)**

---

## Dependency Check

**React Query Dependency:** ❌ **NOT FOUND**

```bash
$ grep -i "react-query\|@tanstack/react-query" package.json
# No results - dependency is missing!
```

Before refactoring to use React Query, you must:
```bash
pnpm add @tanstack/react-query
```

---

## Recommendations (Priority Order)

### 🔴 BLOCKING (Must fix before approval)

1. **Add error state to TransferConversionResult interface**
   - Add `error: Error | null` property
   - Update hook to capture and expose errors
   - Update tests

2. **Add comprehensive JSDoc documentation**
   - Document hook purpose, parameters, and return values
   - Include usage example
   - Document each property in return type

3. **Refactor to use React Query**
   - Install `@tanstack/react-query` dependency
   - Replace manual state management with `useQuery`
   - Significantly simplify the implementation
   - Update tests to use QueryClient provider
   - Remove manual debouncing (React Query handles caching)

### 🟡 RECOMMENDED (Before merge)

1. Verify package.json includes React Query after installation
2. Update test mocks to work with QueryClientProvider
3. Consider adding error retry logic via React Query's `retry` option
4. Document the NET_VALUE_THRESHOLD_PERCENT logic in a comment

---

## Conclusion

**Current Status: ❌ FAILED**

The implementation is functionally sound but **fails to meet three explicit "Must" requirements**:
- ❌ No React Query usage
- ❌ No error state in return type
- ❌ No JSDoc documentation

**Action Required:** The developer must address all three critical issues before this task can be approved. These are not stylistic suggestions—they are specified requirements that must be met.

**Estimated Remediation Time:** 1-2 hours
- Adding JSDoc: 15 minutes
- Adding error state: 15-30 minutes  
- Refactoring to React Query: 45-60 minutes
- Updating tests: 30-45 minutes

**Next Steps:**
1. Install React Query dependency
2. Add error state to interface and hook
3. Add JSDoc documentation
4. Refactor hook to use React Query
5. Update tests accordingly
6. Re-run tests to verify 80%+ coverage maintained
7. Submit for re-review

